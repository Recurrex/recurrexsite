# Project Memory

## Core
Brand palette is reddish gradient via `--brand-1..4` HSL tokens in `src/index.css`; never reintroduce blue/purple gradients. Use `bg-brand-gradient` and semantic tokens — no hardcoded hex in components.
Crew member names link to `/crew/:slug` which renders a personal portfolio page (see crew-portfolio-template).

## Memories
- [Crew portfolio template](mem://features/crew-portfolio-template) — Bento-grid personal portfolio spec for /crew/:slug pages, sections, styling, no Download CV